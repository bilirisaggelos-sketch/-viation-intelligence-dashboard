from collectors.telegram.template import collect_channel


async def collect():

    return await collect_channel(
        channel="Middle_East_Spectator",
        publisher="Middle East Spectator"
    )